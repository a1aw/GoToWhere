import { MAP_REGION_CHANGED, MAP_REGION_CHANGE_COMPLETED } from '../actionTypes';

const initialState = {
    mapRegionChanged: false,
    mapRegionChangeCompleted: true
}

export default function(){

}